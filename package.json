{
  "name": "lucille-3",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first weekly efficiency reporting for AI transformation.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "node scripts/check-js.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
